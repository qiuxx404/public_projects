<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qiuxx.common.web.dao.BrushAccessDao">

    <!-- 查询QXX_CSDN url列表 -->
    <select id="getWebUrlList" resultType="com.qiuxx.common.web.entity.CfgEnumDict">
        SELECT enum_id,enum_value FROM cfg_enum_dict WHERE is_deleted='N'
    </select>

    <!-- 批量更新博客url请求数字 -->
    <update id="updateStatisticsNumList" parameterType="java.util.List">
        UPDATE cfg_enum_dict SET statistics_num=statistics_num+1 WHERE enum_id IN
        <foreach collection="list" index="index" item="item"
                 separator="," open="(" close=") AND ">
            #{item,jdbcType=BIGINT}
        </foreach>
        is_deleted='N'
    </update>

    <!-- 单个更新博客url请求数字-->
    <update id="updateStatisticsNum">
        UPDATE cfg_enum_dict SET statistics_num=statistics_num+1 WHERE enum_id = #{enumId}
    </update>
</mapper>